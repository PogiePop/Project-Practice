<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.audit.mapper.TemplateMapper">
    <select id="findList" resultType="map">SELECT template_id, template_name, plan_type, version, description FROM audit_template WHERE 1=1<if test="planType != null"> AND plan_type=#{planType}</if><if test="keyword != null"> AND template_name LIKE CONCAT('%',#{keyword},'%')</if> ORDER BY create_time DESC</select>
    <select id="findById" resultType="map">SELECT * FROM audit_template WHERE template_id=#{templateId}</select>
    <select id="findByBatchId" resultType="map">SELECT t.template_id, t.template_name, t.plan_type, t.version, t.description FROM audit_template t INNER JOIN batch_template bt ON t.template_id=bt.template_id WHERE bt.batch_id=#{batchId}</select>
    <insert id="bindToBatch">INSERT IGNORE INTO batch_template (batch_id, template_id) VALUES (#{batchId},#{templateId})</insert>
    <delete id="unbindFromBatch">DELETE FROM batch_template WHERE batch_id=#{batchId} AND template_id=#{templateId}</delete>
</mapper>
