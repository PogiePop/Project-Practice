<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.audit.mapper.CareerHistoryMapper">
    <select id="findByLeaderId" resultType="map">SELECT record_id, leader_id, unit_name, position, start_date, end_date, duty_description, fund_scope, source FROM leader_career_history WHERE leader_id=#{leaderId} ORDER BY start_date DESC</select>
    <insert id="insert">INSERT INTO leader_career_history (record_id, leader_id, unit_name, position, start_date, end_date, duty_description, fund_scope, source) VALUES (#{recordId},#{leaderId},#{unitName},#{position},#{startDate},#{endDate},#{dutyDescription},#{fundScope},'手动录入')</insert>
    <update id="update">UPDATE leader_career_history SET unit_name=#{unitName}, position=#{position}, start_date=#{startDate}, end_date=#{endDate}, duty_description=#{dutyDescription}, fund_scope=#{fundScope} WHERE record_id=#{recordId} AND leader_id=#{leaderId}</update>
    <delete id="delete">DELETE FROM leader_career_history WHERE record_id=#{recordId} AND leader_id=#{leaderId}</delete>
</mapper>
